A script for QGIS that determins a stochastic viewshed by running multiple iterations of the viewshed with parts of the canopy randomly removed

This simulates how vegetation is not entirely opaque to a viewer

It also visually describes the uncertainty of the data
